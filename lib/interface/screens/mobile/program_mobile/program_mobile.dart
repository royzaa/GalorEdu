import 'package:flutter/material.dart';

class ProgramMobile extends StatelessWidget {
  const ProgramMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
